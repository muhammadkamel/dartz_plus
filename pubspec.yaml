name: dartz_plus
version: 0.0.1
homepage: https://github.com/muhammadkamel/dartz_plus
description: Functional Programming in Dart.

environment:
  sdk: ^3.10.1

dependencies:
  equatable: ^2.0.7
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
