name: dartz_plus_example
description: Example Flutter app demonstrating dartz_plus usage
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter
  dartz_plus:
    path: ../
  equatable: ^2.0.7
  dio: ^5.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.6
  dartz_plus_generator:
    path: ../dartz_plus_generator

flutter:
  uses-material-design: true
